# ShengHangCopilot
ShengHangCopilot

## 使用方法

### 1. 启动后端服务

首先需要启动Django后端服务：

```bash
python manage.py runserver
```

### 2. 启动前端服务

启动前端服务：

```bash
python -m http.server 3000
```

### 3. 访问前端页面

```bash
访问 http://127.0.0.1:3000
```





## 目前尚未实现的功能（其中带*的建议在原方法上添加）

### 用户管理模块

个人信息的显示属性(是否可见)*

举报用户

对于封禁用户的登录处理（不能登录或是什么其他限制）*

### 歌手与音乐管理模块

支持对搜索结果按某种属性排序（例如按播放次数或收藏量排序）*

### 收藏与歌单模块

增加对平台歌手的排行榜（关注数或其他属性）*

支持除了按标题以外的歌单搜索*

收藏列表的筛选查看*

举报歌单

### 评论与互动模块

支持取消点赞*

### 播放记录模块

（可选）生成“用户自动推荐歌单”（根据用户播放历史自动生成）

### 管理员管理模块

系统日志管理（对管理员操作进行记录，如增删歌曲、处理举报等）（体现在对系统日志表的添加）

查看系统日志

处理各种举报：用户、歌单、评论等（删除或封禁等）

屏蔽或锁定特定评论区

支持修改歌手、专辑、歌曲的信息

查看用户行为统计（某段时间内）





**以上是暂时未实现的功能，主要是管理员管理模块`manager.py`**